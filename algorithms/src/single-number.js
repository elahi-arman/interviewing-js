/**
  @name {singleNumber}
  @param {Array<number>}
  @returns {number}
  @description Every element appears twice except for one. Find that single one.
**/

const singleNumber = function (nums) {
  // O(n) time -> only single for loops
  // O(1) space -> no hashmap
  let sum = 0;
  for (let i = 0; i < nums.length; i++) {
  }
};
